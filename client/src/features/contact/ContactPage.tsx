import { Typography } from "@mui/material";

export default function ContactPage() {
    return (
        <Typography variant="h2">
            ReactSharpStore Contact Page
        </Typography>
    );
}