import { Typography } from "@mui/material";

export default function ProductDetails() {
    return (
        <Typography variant="h2">
            ReactSharpStore Product Details
        </Typography>
    );
}